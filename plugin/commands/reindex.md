---
description: Trigger code reindexing for the retrieval backend
---

Trigger reindexing:

```bash
stratus reindex $ARGUMENTS
```

Runs an incremental reindex of the codebase for the Vexor retrieval backend.

Options:
- `--full` — clear and rebuild the entire index from scratch
