services:
  devrag:
    container_name: better_claude_devrag
    mem_limit: 512m
    restart: unless-stopped
    build:
      context: .
      dockerfile: docker/devrag/Dockerfile
    volumes:
      - .:/app
